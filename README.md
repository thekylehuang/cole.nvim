# Cole.nvim

A port of the stationery-inspired colorscheme Cole

## Installation

### With `lazy.nvim`

```lua
{
    "thekylehuang/cole.nvim",
    priority = 1000,
    config = function()
        vim.cmd.colorscheme("cole")
    end,
}
```
